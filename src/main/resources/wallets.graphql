schema{
    query: Query
}

type Query{
    allWallets: [Wallet]
    wallet(id: String): Wallet
}

type Wallet {
    walletId: String!
    userId: String!
    walletName: String
    walletType: String!
}